# This file is rendered locally by: op-refresh
# Example values. Adjust vault/item/field names to your own 1Password entries.

[user]
    email = "{{`{{ op://`}}{{ .op.vault | default "Private" }}/{{ .op.github_item | default "GitHub" }}/email{{` }}`}}"
    # Example SSH signing key reference kept local.
    # Replace with your own preferred public-key field or generated path.
    signingkey = "{{ .ssh.default_identity_public_key | default "~/.ssh/id_ed25519.pub" }}"
