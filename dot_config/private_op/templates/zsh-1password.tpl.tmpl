# Machine-local environment variables rendered via op inject.
export GITHUB_TOKEN="{{`{{ op://`}}{{ .op.vault | default "Private" }}/{{ .op.github_item | default "GitHub" }}/token{{` }}`}}"
export WORK_GITHUB_TOKEN="{{`{{ op://`}}{{ .op.vault | default "Private" }}/{{ .op.work_github_item | default "GitHub Work" }}/token{{` }}`}}"
