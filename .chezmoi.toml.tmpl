{{- $name := promptString "Git full name" (dig "git" "name" "Timo Sugliani" .) -}}
{{- $email := promptString "Git email" (dig "git" "email" "timo.sugliani@gmail.com" .) -}}
{{- $use_1password := promptBool "Enable 1Password (y/n)" (dig "use_1password" false .) -}}

[data.git]
    name = {{ $name | quote }}
    email = {{ $email | quote }}

[data]
    use_1password = {{ $use_1password }}
